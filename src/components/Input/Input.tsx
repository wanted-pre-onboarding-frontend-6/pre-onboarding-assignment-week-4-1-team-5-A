// import { inputStyleProps } from 'libs/types/style/stylePros.type';
// import { FC } from 'react';
// import * as Styled from './Style';

// const Input: FC<inputStyleProps> = ({ type, variant, shape, size, ...rest }) => {
//   return type === 'input' ? (
//     <Styled.Input variant={variant} shape={shape} size={size} {...rest} />
//   ) : (
//     <Styled.TextArea variant={variant} shape={shape} size={size} {...rest} />
//   );
// };
// export default Input;

const Input = () => {
  return <div></div>;
};
export default Input;
