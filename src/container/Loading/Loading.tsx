import Spinner from 'components/Spinner/Spinner';
import * as Styled from './Style';
const Loading = () => {
  return (
    <Styled.Wrapper>
      <Spinner />
    </Styled.Wrapper>
  );
};
export default Loading;
